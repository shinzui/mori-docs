---
title: Changelog
description: Notable changes and new features in mori
icon: ScrollText
---

All notable changes to mori, organized by date. This changelog covers user-facing
features, commands, and schema changes.

---

## 2026-04-28

### `mori registry exec` (new)
- Run the same command in every registered project, with `cwd` set to each project's local path
- Filters: `--group`, `--namespace`, `--type`, `--origin`, `--filter GLOB` (compose, AND semantics)
- `--jobs N` for parallel execution; `--fail-fast` to abort at the first failure
- `--dry-run` and `--json` modes; per-project summary table with status, exit code, and duration
- Skips projects without a local checkout; exits 0 if every executed command succeeded, 1 otherwise

### `mori reaction recover` (new)
- Unstick a reaction stuck at `status: triggered` (left over from pre-fix daemon builds that didn't catch executor exceptions)
- Marks the row `failed`, advances the executor checkpoint past the stuck event
- `--retry` re-emits a fresh `ReactionTriggered` carrying the original actions under a new reaction ID
- Refuses to operate on rows already in `completed` or `failed` state

### `mori reaction show`
- Failed `RunCommand` actions now display dedicated `Stdout:` and `Stderr:` blocks captured from the failed process

### `mori automate register` / `update`
- One-step registration: a single `mori automate register` emits both `ProjectRepoLinked` and `AutomationRegistered`, so a separate `mori observe` is no longer required to receive signals
- `mori automate update` heals state from older daemon builds by re-emitting `ProjectRepoLinked` if the projects row is missing
- `--path <dir>` is authoritative across `register`, `update`, `deregister`, `run`, and `ingest git`: the config file is read from `<dir>` and the repo TypeID is derived from `<dir>`, making it impossible to silently register the wrong repo

### Automation: nix devshell wrapping
- Every `RunCommand` is now spawned as `nix develop --command <command> <args…>` against the **target** repo's flake
- Each automation-target repo must have a `flake.nix` exposing a default devShell with the binaries the reaction references on `nativeBuildInputs`
- Removes the need to wrap commands in `/bin/bash -lc "cd … && …"` to pick up nix-installed tools under launchd

### Automation: executor fault tolerance
- The executor subscription wraps `executeActions` in an exception boundary; an uncaught error during action execution is recorded as a `ReactionFailed` event instead of halting the subscription
- Reaction history persists captured stdout/stderr per action result on `mori.reaction_history`

### `mori register --no-seihou-discovery` (new flag)
- Skip the auto-discovery probe for seihou template manifests when the project root holds a `module.dhall` that isn't a seihou module instance (e.g. a seihou type-definition file) and the probe produces noisy "Missing record field" warnings
- Internal callers (`mori observe`, `mori automate register`, `mori registry reregister`) keep the previous behavior

### `mori help`
- New `--width N` flag (with auto-detect, capped at 140) wrapping output for both direct topic rendering and the fzf preview

### Schema: cookbook extension import migration
- `upgrade-cookbook-extension-import` migration normalizes `let Cookbook = …` bindings to the canonical hashed form pinned in lockstep with the root `Schema`
- Rewrites un-hashed `let ContentType = …` / `let Topic = …` direct imports (which fail at runtime with `TlsNotSupported`), bumps stale-pinned `Cookbook` bindings, and injects the binding when files use `Schema.{ContentType,Topic}` references the root package never re-exported
- Runs as part of `mori schema migrate` on `mori/cookbook.dhall` and as part of `mori registry upgrade-schema`'s bulk pipeline

### Test fixtures: automation test repos
- New `just bootstrap-test-repos` / `just reset-test-repos` recipes generate three persistent sibling repos (publisher, happy-path consumer, failure-path consumer) for end-to-end automation repros
- Bootstrap script enforces dev-database-only — refuses to run against `~/.mori/db`

### Cross-repo workflows
- `mori workflow list` / `trace` now display the qualified `namespace/name` source project and the upstream `Source Reaction` whose `Signal` action emitted the workflow
- `SignalSelector.sourceProjects` requires the qualified `namespace/name` form; bare project names will silently match nothing

---

## 2026-04-18

### Schema: mk-pattern refactor
- Every core schema record is now published as a four-field bundle: `{ Input, Type, default, mk }`
- User configs are written with Dhall's record-completion operator: `Schema.Project::{ … }`, `Schema.Package::{ … }`, `Schema.Repo::{ … }`, etc.
- Fields that equal the published default can be **elided**; additive schema changes no longer break existing configs
- Extension records follow the same convention: `Cookbook.CookbookCatalog::{ … }`, `TechRadar.TechRadar::{ … }`, with per-extension `package.dhall` re-exports under `schema/extensions/<name>/`
- Automation schema (`Reaction`, `EventSelector`, `ReactionAction`, `ExecutionPolicy`) remains pre-mk for now and is a known gap

### `mori schema migrate`
- **`rewrite-to-mk-form`** migration — rewrites legacy bare-record `mori.dhall` into `Schema.Project::{ … }`, wraps inner records, elides default-equal fields, and `.Type`-qualifies annotations where needed
- Handles extension files (`mori/cookbook.dhall`, `mori/tech-radar.dhall`) — detected by path, with per-kind migration pipelines
- Parse-guarded steps: a step whose output doesn't parse is auto-reverted so later migrations still run
- Fixed four anchor / scan bugs that corrupted unusual-but-valid Dhall inputs

### `mori registry upgrade-schema` (new)
- Walks the registry and upgrades all projects whose `schema_hash` diverges from the CLI pin
- Flags: `--dry-run`, `--filter GLOB`, `--all`, `--commit`, `--allow-dirty`
- Covers `mori.dhall` **and** known extension files in a single pass
- One project's failure does not abort the others

### `mori registry` additions
- **`mori registry templates list/show`** — browse seihou templates across the registry at the *template* grain, with `--tag`, `--namespace`, and three-segment `<namespace>/<name>/<template>` references
- **`mori registry group create/list/show/add/remove/attach-package/attach-doc/…`** — first-class groups: named, event-sourced collections of projects with optional attached packages, docs, and JSON extension data
- `registry list` gains `--group`, `--domain`, and `--observed` filters
- Projects track `schema_url` / `schema_hash` and emit `ProjectSchemaVersionChanged` events

### `mori path`
- **`--copy` / `-c`** — copy the resolved path to the system clipboard (pbcopy / xclip) in addition to printing it

### `mori agent bootstrap`
- **`--seihou`** flag — bootstrap a project that ships seihou templates; adds the `seihou` CLI to the session allow-list and injects template authoring guidance into the system prompt

### `mori automate daemon`
- Periodic **auto-ingest loop** — ingests git history for every observed project (any project with a `repo_id`), independent of whether it has an automation config
- New `--ingest-interval SECONDS` flag (default: 300, `0` to disable)
- Daemon now manages six concurrent subscriptions (added app-registration stream)

### Cookbook extension
- **`CookbookEntry.tags`** — new optional field (`List Text`) for cross-cutting tags
- `addCookbookEntryTags` migration brings existing catalogs forward

---

## 2026-03-31

### Cookbook extension
- **`mori cookbook list`** — list classified code examples, patterns, and guides with filtering
- **`mori cookbook show`** — show details for a cookbook entry
- **`mori cookbook print-schema`** — print the cookbook Dhall schema
- **`mori extension query cookbook`** — cross-project cookbook queries with `--project`, `--topic`, `--content-type` filters
- Cookbook catalog included in `mori agent context` output for AI agent access
- Rich structured cookbook entries in `mori registry show --full`
- `mori/cookbook.dhall` config file with content types (SampleCode, Instructions, Pattern, Configuration, Template) and topics (Database, API, Testing, Effects, etc.)

### Mori Apps (webhook delivery)
- **`mori app register`** — register webhook receivers for event delivery
- **`mori app list`**, **`show`**, **`update`** — manage app lifecycle
- **`mori app rotate-secret`** — rotate HMAC signing secret
- **`mori app suspend`** / **`activate`** — pause and resume delivery
- **`mori app delete`** — remove an app
- **`mori app deliveries`** — view delivery history with status filtering
- **`mori app retry`** — retry failed deliveries
- Standard Webhooks v1 HMAC-SHA256 signing
- Exponential backoff retries (5s to 24h, 10 attempts)
- Durable queue via pgmq, automatic 410 Gone suspension

### Kit command
- **`mori kit list`** — list available skills and subagents
- **`mori kit install`** — install to user or project scope
- **`mori kit update`** — update installed items
- **`mori kit uninstall`** — remove skills/subagents
- **`mori kit status`** — show installed status
- Skills and subagents automatically loaded into `mori agent claude` sessions

### Trailer matching and event-data interpolation
- `trailerPatterns` field on `ChangesetSelector` — match commits by git trailer key/value pairs
- `{{variable}}` template syntax for action fields — expanded at trigger time
- Variables: `{{changeset.id}}`, `{{changeset.subject}}`, `{{trailer.<Key>}}`, `{{ref.name}}`, etc.
- Resolved values appear in reaction history

### apiSource field
- New `apiSource` field on Package records for Client-type packages
- Declares which API a client was generated from (`project`, `api`, `specPath`)
- Displayed in `mori show --full` and `mori registry show --full`
- Validation: `apiSource.project` must appear in project-level `dependencies`

### Agent automate command
- **`mori agent automate`** — AI-assisted automation config authoring and debugging

### Registry enhancements
- **`mori registry remove --dry-run`** — preview removal before executing
- `registry remove` now cleans up package_dependencies and warns about dependents
- Package-qualified show: `mori registry show project:package`
- Project and package descriptions in `registry show --full`

### Validate improvements
- **`mori validate --check-deps`** — validate dependencies against the local registry
- `apiSource.project` must appear in project-level dependencies

### Extension enhancements
- `mori extension query` now supports `--project`, `--topic`, `--content-type` filters
- **`mori tech-radar print-schema`** — print tech-radar Dhall schema
- Auto-register extensions during project registration
- Removed `--for-agent` flag from `mori schema print`

### Browse extended
- `mori browse` now accepts `project:package` syntax for package-qualified lookups

### Interactive selection
- fzf interactive project selection for `mori cd` and `mori path`
- fzf interactive topic selection for `mori help`

---

## 2026-03-21

### Dynamic extension registry
- **`mori extension define`** — define extension types in a catalog for schema extensions
- **`mori extension list`** — list active extensions
- **`mori extension retire`** — soft-delete an extension (preserves existing data)
- **`mori extension query`** — query extension data across all projects
- Extension data flows through the Project aggregate event-sourcing pipeline
- Stored as JSONB in `mori.project_extensions` with GIN indexing
- Compile-time loader registry maps extension names to typed Haskell loaders (tech-radar is the first adapter)
- `mori help extensions` topic documenting the extension system
- `mori registry show --full` now includes extension data

### Skills and subagents in registry
- Skills and subagents from `mori.dhall` now persist through the full event-sourcing pipeline
- 6 new granular event types: `ProjectSkillAdded`/`Removed`/`Updated`, `ProjectSubagentAdded`/`Removed`/`Updated`
- `mori.skills` and `mori.subagents` database tables with subscription projection
- `mori register` extracts skills/subagents from config and diffs against current state
- `mori registry show --full` displays skills and subagents (text + JSON output)
- Project events now at 22 types (was 11)

---

## 2026-03-19

### Event-driven daemon registration
- **AutomationRegistration aggregate** — daemon now uses event-driven registration instead of filesystem polling
- `mori automate register`, `deregister`, `update`, `list`, `migrate` commands for managing automation configs
- Daemon subscribes to `automationRegistration` category for live config updates

### Registry open-doc command
- **`mori registry open-doc`** — open project docs in your editor with fzf-based interactive selection
- Supports project and doc selection via fzf with bat-powered previews
- Uses `$EDITOR` / `$VISUAL` for local files, system `open` for URLs

### Claude agent sessions
- **`mori agent claude`** — start a Claude session with full project context, skills, and subagents
- Loads skills and subagents from `~/.config/mori/agents/` and `.mori/agents/` via `--add-dir`
- Context caching for faster subsequent launches

---

## 2026-03-17 – 2026-03-18

### Package-qualified dependency syntax
- Colon syntax for targeting specific packages: `mori deps explain hasql:hasql-pool`
- Works with `deps locate`, `deps explain`, and dependency declarations

### Cross-repo automation help
- `mori help cross-repo-automation` topic covering signals, workflows, and tracing

### Workflow decider pattern
- Workflow aggregate now follows full decider pattern with command/event separation
- Daemon runs registry and workflow subscriptions even without automation configs

---

## 2026-03-15

### Tech radar extension
- **`mori tech-radar list`** — list recommended packages by language and category
- **`mori tech-radar show`** — show adoption level and alternatives for a package
- Dhall schema for tech radar config (`mori/tech-radar.dhall`)
- Four adoption levels: `Adopt`, `Trial`, `Assess`, `Hold`

### Agent claude command
- **`mori agent claude`** — start a Claude session with mori project context for this project
- Cached dependency resolution for faster startup

---

## 2026-03-14

### Multi-repo automation daemon
- **`mori automate daemon`** — manage automation for all registered repos from a single process
- Discovers repos from the project registry, loads configs, routes events automatically
- Periodic config refresh with `--refresh-interval` flag
- See [Automation Daemon guide](/docs/guides/automation-daemon)

### Reaction history commands
- **`mori reaction list`** — list recent reactions with filtering by status and name
- **`mori reaction show`** — inspect full details for a single reaction
- See [Reaction History guide](/docs/guides/reaction-history)

### Agent schema migration
- **`mori agent schema-migrate`** — AI-assisted schema migration that validates and fixes `mori.dhall` automatically

### Bug fixes
- Fix reaction executor subscription silently failing on startup

---

## 2026-03-13

### Runtime environment type
- Add `RuntimeEnvironment` type to schema for package execution runtime configuration

### Shell completions overhaul
- Replace manual completion registry with optparse-applicative built-in protocol
- Completions now automatically stay in sync with command definitions

---

## 2026-03-12

### Path command
- **`mori path`** — print local project path with existence check
- `--package NAME` and `--doc KEY` flags to locate specific packages or docs
- FZF integration with `--pick` for interactive selection

### Schema migrations
- **`mori schema migrate`** — first-class migration system for schema evolution
- Migrations for `skills`, `subagents`, and package `lifecycle` fields
- `--apply`, `--dry-run`, and `--file` flags

### Command aliases
- Add aliases help topic to CLI help system
- See [Aliases guide](/docs/guides/aliases)

### Compile-time schema embedding
- Schema definitions embedded at compile time for `mori schema print`
- Agent prompts now use `mori schema print` instead of inline embedding

---

## 2026-03-11

### Skills and subagents schema
- **`skills`** field — define agent skill definitions following the agentskills.io spec
- **`subagents`** field — define specialized AI assistants with provider, model, tools, and permission configuration
- `PermissionMode` type: `Default`, `AcceptEdits`, `DontAsk`, `BypassPermissions`, `Plan`
- Package **`lifecycle`** field — `Active`, `Experimental`, `Deprecated`, `Archived`
- Deprecated lifecycle displayed in pale red in `show` and `registry show` commands
- Skills and subagents integrated into `agent bootstrap` prompts, schema catalog, and `init` scaffold
- See [Schema Reference](/docs/guides/schema-reference) for full field documentation

### Schema upgrade improvements
- `--latest` flag for `mori schema upgrade` to resolve from GitHub

---

## 2026-03-10

### Registry enhancements
- Resolve full paths for local docs in `registry show --full`

---

## 2026-03-03 – 2026-03-09

### Schema as git submodule
- Replace `schema/` directory with git submodule pointing to [mori-schema](https://github.com/shinzui/mori-schema)
- Project Origin type (`Own`, `ThirdParty`, `Fork`, `Vendored`) and enriched Dependency metadata

### Cross-repo workflows
- Signal-based workflow system for cross-repository automation
- `SignalSelector` event selector for receiving typed signals
- `*dependents*` dynamic target resolution
- `mori workflow list`, `mori workflow status`, `mori workflow trace` commands
- See [Cross-Repo Workflows guide](/docs/guides/cross-repo-workflows)

### First-class schema migration system
- `mori schema migrate` with `--apply` and `--dry-run` modes
- Migration for `origin` field and augmented `Dependency` type

### Documentation references in registry
- Docs-only projects now discoverable via `registry list`, `search`, `show`

### Agent bootstrap corpus mode
- `--corpus` flag for `agent bootstrap` to set up corpus project contexts

---

## 2026-02-27 – 2026-03-01

### Dependency tree visualization
- **`mori deps tree`** — recursive dependency tree with colored nodes

### CLI beautification
- Colored, styled output for all CLI commands
- `Pretty` module foundation for consistent output formatting
- Styled output for `doctor`, `show`, `registry list/show/search`, `deps resolve/locate/explain/tree`

### Agent enhancements
- `--allowedTools` and `--permission-mode` flags for `agent bootstrap` and `agent assist`
- Registry-based dependency context injected into agent assist prompts
- Enhanced `registry search` to include package name matches

### Shell completions
- Shell completion support for bash, zsh, and fish
- `install-completions-zsh` recipe

---

## 2026-02-25 – 2026-02-26

### Shell integration
- **`mori cd`** — change directory to a registered project
- Bare project name support with ambiguity detection
- See [Shell Integration guide](/docs/guides/shell-integration)

### Schema commands
- **`mori schema print`** — display embedded schema definitions
- **`mori schema upgrade`** — upgrade to latest schema version with remote resolution
- Embedded help topics for schema records, types, and modification

### Agent commands
- **`mori agent assist`** — launch Claude with mori project context
- **`mori agent bootstrap`** — registry-aware project setup
- `--debug` flag to print system prompt

### Registry query commands
- **`mori registry list`** — list registered projects with `--namespace` filtering
- **`mori registry show`** — show project details
- **`mori registry search`** — search across projects

### Show command
- `--full` flag for detailed section output

### Version info
- Git SHA included in `mori --version` output

---

## 2026-02-24

### Nix build support
- Global mori binary installation via Nix
- Inline projection after `register` for immediate read model population
- `MORI_PG_CONNECTION_STRING` replaces `PG_CONNECTION_STRING`

---

## 2026-02-14 – 2026-02-18

### Testing and stabilization
- Golden tests for event JSON serialization across all aggregates
- Upgrade to hasql 1.10 with Tier 2 integration tests
- Fix `<<loop>>` in FromDhall for recursive ReactionAction/ScheduleAction
- Fix `automate run` startup issues (checkpoints table, ConcUnlift, executor parsing)

### Event sourcing improvements
- TypeID-based stream identity for Project aggregate
- Daily stream partitioning for Repository aggregate
- Registry subscription and config projection (Milestone 1.3.2, 1.6.2)
- Scheduled action execution via pgmq + shibuya (Milestone 1.6.4)

---

## 2026-02-11 – 2026-02-12

### Phase 1 core implementation

All Phase 1 milestones completed:

- **Milestone 1.1** — Foundation infrastructure (PostgreSQL + MessageDB, effect stack, TypeID)
- **Milestone 1.2** — Schema & Validation (`mori init`, `mori validate`, `mori show`)
- **Milestone 1.3** — Local Registry (`mori register --local`, dependency resolution)
- **Milestone 1.4** — Agent Context (`mori agent context <role>`)
- **Milestone 1.5** — VCS Ingestion (`mori automate ingest`)
- **Milestone 1.6a** — Automation Engine (`mori automate run`, event selectors, reactions)
- **Milestone 1.6b** — Automation Observability (`mori automate inspect`, `mori automate explain`)

See [Roadmap](/docs/roadmap) for milestone details.

---

## 2026-01-21 – 2026-02-01

### Project inception
- Nix flake and multi-package Haskell project structure
- Architecture documentation: CLI, Registry, Schema, Evaluation
- Dhall schema design for project identity layer
- Event sourcing architecture with MessageDB
- Automation system design
- Agent context assembly architecture
- Implementation roadmap
