---
title: Event Sourcing
description: Mori's current event runtime, read models, queues, and migration boundary
icon: Database
---

## Overview

Mori is event-sourced: commands append immutable events, current state is
derived by folding those events, and user-facing queries read PostgreSQL
projections. The current runtime is built around the keiro-runtime family:

- **kiroku** stores ordered PostgreSQL event streams and exposes subscription
  delivery.
- **keiro** runs command handlers, routers, process managers, durable timers,
  and async projections on top of those streams.
- **keiki** supplies the pure transducer model used by aggregates and routers.
- **shibuya** supervises long-running processors.
- **pgmq** backs delayed work, FIFO queued automations, webhook delivery, and
  dead-letter queues.

Kiroku is the only event store. The legacy MessageDB path — its streams, its
compatibility check in `mori doctor`, and the `mori history` transformation
command — has been retired.

## Runtime Shape

```text
PostgreSQL
├── kiroku schema
│   ├── streams
│   ├── events
│   └── stream_events
├── keiro runtime tables
│   ├── projection deduplication
│   ├── durable workflow state
│   └── timer state
├── pgmq schema
│   ├── scheduled_actions
│   ├── scheduled_reactions
│   ├── queued_reactions
│   └── dead-letter queues
└── mori schema
    ├── projects
    ├── packages
    ├── package_dependencies
    ├── automation_registrations
    ├── reaction_history
    ├── pending_scheduled_reactions
    ├── workflows
    ├── groups
    ├── apps
    ├── okf_bundles
    ├── okf_concepts
    ├── okf_concept_edges
    ├── repository_ingest_status
    ├── changeset_content_index
    ├── ddd read models
    ├── tech-radar read models
    └── agent-plan read models
```

The exact table set is migration-owned and can grow as extensions add read
models. User commands should treat the `mori` schema as the query surface and
avoid depending on private runtime tables.

## Streams And Contexts

Mori organizes events by bounded context. The current supported migration
contexts are:

```text
app
automation_registration
group
project
reaction
repository
workflow
```

Typical stream categories:

| Context | Purpose |
|---|---|
| `project` | Project registration, metadata, dependencies, docs, packages, bundles, extensions |
| `repository` | VCS observations, config observations, workflow signals, ingest health events |
| `reaction` | Reaction lifecycle: triggered, scheduled, skipped, failed, cancelled, completed |
| `workflow` | Cross-repo workflow lifecycle and step state |
| `automation_registration` | Daemon registration, update, and deregistration events |
| `group` | Registry group membership |
| `app` | Webhook app registration and delivery state |

Kiroku stores events in named streams and also records an ordered global log.
Subscriptions can consume categories and consumer groups through the
keiro/shibuya worker host.

## Event Provenance

New events carry additive append-time provenance beside their immutable
payload and codec metadata:

- `actor` identifies the CLI, daemon, specific worker, API, or generic process;
- `causer` distinguishes a recorded Kiroku event from a timer or message;
- `correlation` reuses the cross-project workflow correlation envelope; and
- Kiroku's OpenTelemetry bridge records W3C `traceparent` and optional
  `tracestate`.

Only a recorded event cause is promoted into Kiroku's indexed
`causation_id`. Timer and message identities remain metadata-only. Workflow
correlation promotes the workflow TypeID UUID into `correlation_id` while
retaining its full target, delivery, depth, and digest-chain evidence.

Provenance is not backfilled. Historical or malformed optional values remain
`not recorded`. Inspect a reaction's envelope with
[`mori reaction show`](/docs/commands/reaction#mori-reaction-show), then follow
an event edge with
[`mori ops stream causation`](/docs/commands/ops#mori-ops-stream-causation).

## Read Models

Read models are PostgreSQL tables derived from event streams. They make the CLI
fast and predictable:

- `mori registry`, `mori deps`, `mori cd`, `mori path`, and `mori browse` query
  project/package/dependency projections.
- `mori reaction` queries reaction history and pending scheduled reactions.
- `mori workflow` queries workflow projections.
- Extension commands such as `mori plans`, `mori tech-radar search`, `mori ddd
  search`, and `mori registry concepts` query extension-specific projections.

Read models are rebuildable from events. If an owned projection is stale, stop
its writers, inspect its catalog group with `mori ops rebuild`, and use the
preview-first rebuild workflow. Restarting the daemon does not repair a catalog
or replay an inline projection.

## Worker Host

The multi-repo daemon runs the current reactive runtime:

```text
kiroku streams
    |
    v
shibuya processors
    |
    +-- keiro routers
    +-- keiro process managers
    +-- keiro async projections
    +-- durable timer workers
    +-- bespoke reactors for app fan-out and scheduled action enqueue
```

The worker host gives mori supervised processing, graceful shutdown, retry
visibility, and backpressure without each feature hand-rolling its own loop.
Router and process-manager legs are idempotent by construction; async
projections use keiro projection deduplication.

## Queues

Mori uses pgmq for delayed and queued work:

| Queue family | Used for |
|---|---|
| `scheduled_actions` | Legacy per-action `Schedule` wrapper |
| `scheduled_reactions` | Preferred per-reaction schedule lifecycle |
| Queued reaction jobs | `queued = True` automation reactions with FIFO ordering per automation |
| `webhook_deliveries` | Mori app webhook delivery and retry |
| Dead-letter queues | Failed scheduled/queued jobs that exhausted retry policy |

Scheduled action and scheduled reaction queues use keiro-pgmq `Job`
definitions, so the queue name, JSON codec, retry policy, and dead-letter
configuration live with the job definition. Webhook delivery still uses its
bespoke pgmq adapter.

## Verifying and Rebuilding Derived State

Because read models are derived, they are also reproducible — which makes both
verification and repair ordinary operations rather than emergencies.

```bash
mori ops replay-audit --full            # read-only: do streams still fold cleanly?
mori ops rebuild list                   # which rebuild groups exist?
mori ops rebuild preview GROUP          # what would a rebuild touch?
mori project audit-functional-streams   # fixed audit over the functional aggregates
```

[`mori ops replay-audit`](/docs/commands/ops#mori-ops-replay-audit) writes
nothing; it hydrates streams and reports failures and seed divergences, which
is what you run after touching a codec, an upcaster, or a fold. One of
`--full` or `--target` is required — there is no default mode.

[`mori ops rebuild`](/docs/commands/ops#mori-ops-rebuild) is the destructive
counterpart. Its unit is a catalog rebuild *group*, never a single table, and
every mutating step previews first and exits 1 until you re-run with `--force`.

Side-effecting subscriptions — the executor, routers, webhook fan-out — must
never replay from position zero. There is no operator step for this any more:
each reactor declares a missing-checkpoint policy in code, and every mori
reactor declares `FromCurrentHead`.

## Operational Checks

```bash
mori doctor
mori doctor --ingest
mori doctor --reactions
```

`mori doctor` checks the baseline database connection and decodes every active
automation registration. `mori doctor --ingest` checks
`mori.repository_ingest_status`, and `mori doctor --reactions` checks reaction
execution legs; both exit `2` when unhealthy, making them suitable for
monitoring.

For day-to-day runtime health, also watch:

- daemon logs from `mori automate daemon`
- stuck ingest rows through `mori doctor --ingest`
- stuck or halted execution legs through `mori doctor --reactions`
- pending scheduled reactions with `mori reaction scheduled list`
- failed or cancelled reactions with `mori reaction list --status failed`
- failed signal deliveries with `mori signal list --failed`
- queued/dead-letter tables when pgmq jobs exhaust retry policy
- Kiroku subscription lag through `mori automate daemon --metrics-port`

## Design Invariants

1. Events are the durable source of truth.
2. Read models are derived and rebuildable.
3. CLI write paths route through the configured event backend.
4. Long-running work is supervised by the daemon runtime.
5. Deferred work is explicit in pgmq-backed queues.
6. Kiroku is the only event store; MessageDB references belong to retired
   history.
